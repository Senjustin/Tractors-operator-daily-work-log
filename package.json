{
  "name": "tractors-worklog",
  "version": "1.0.0",
  "description": "Tractors Operator Daily Work Log MERN Application",
  "main": "server.js",
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend start\"",
    "start": "npm --prefix backend start",
    "build": "npm --prefix frontend build"
  },
  "keywords": [
    "mern",
    "tractors",
    "worklog",
    "express",
    "react",
    "mongodb"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
